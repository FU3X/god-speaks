on:
  push:
    branches:
    - ${branch}
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v2
    - run: npm install --global yarn
    - run: yarn install
    - run: yarn tauri build
